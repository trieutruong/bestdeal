import styles from 'styles/NewsProduct.module.scss'
import classNames from 'classnames/bind';

const cx = classNames.bind(styles)

const NewsProduct = () => {
  return (
    <div className={cx('wrapper')}>
        
    </div>
  );
};

export default NewsProduct;
